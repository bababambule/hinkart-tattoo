---
import Icons from './Icons.astro';
const { name, slug, video, index } = Astro.props;
const videos = import.meta.glob('../assets/videos/*.webm', { eager: true });
const videoPath = videos[`../assets/videos/${video}`]?.default;
---

<div class="embla__slide h-full relative flex items-center min-w-1/5" data-slide-index={index}>
	<div class="video__wrapper relative bg-daintree-1000 p-2">
		<div class="video__overlay"></div>
		<video muted loop class="object-cover origin-center h-full">
			<source src={videoPath} />
		</video>
	</div>
	<div class="absolute z-10 bottom-1/4 mx-auto w-full flex justify-center">
		<p class="artist__name font-display text-daintree-600 text-5xl -rotate-3" data-name={name}>{name}</p>
		<p class="artist__name--shadow font-display text-daintree-600 text-5xl -rotate-3 absolute" data-name={name}>{name}</p>
	</div>
	<div class="artist__select absolute z-10 bottom-1/6 mx-auto left-1/2 -translate-x-1/2 w-40 h-12">
		<a href={slug} class="text-tree-poppy-100 flex flex-col justify-center px-4 py-2 relative">
			<div class="relative z-10- -mt-2">
				<span class="text-xl uppercase font-black">Select</span>
				<span class="text-xl uppercase font-black text-stroke absolute left-0 z-[-1]">Select</span>
			</div>
			<div class="absolute -right-3 bg-tree-poppy-600 rounded-full p-1">
				<Icons size={40} classes="" icon="ElectricityCircle" />
			</div>
			<span class="font-kanji absolute -bottom-4 text-3xl opacity-60 z-[-2]">えらぶ</span>
			<div class="artist__select-bg bg-tree-poppy-600 absolute w-40 h-12 top-0 -z-10 left-0 -rotate-3"></div>
		</a>
	</div>
</div>

<style>
	.embla__slide {
		min-width: 20%;
		padding-left: var(--slide-spacing);
		transform-origin: center;
	}

	.video__overlay {
		position: absolute;
		z-index: 1;
		width: 100%;
		height: 100%;
		mix-blend-mode: hard-light;
	}

	.artist__select-bg {
		clip-path: polygon(0% 15%, 100% 0%, 95% 90%, 2.5% 100%);
	}

	.text-stroke {
		-webkit-text-stroke: 0.4rem var(--color-tree-poppy-1000);
	}
</style>
